name: Upgrader

on:
  workflow_dispatch:
  schedule:
    - cron: "12 8 5 1-9,11-12 *"

jobs:
  upgrade:
    uses: browniebroke/github-actions/.github/workflows/poetry-upgrade.yml@2f5f807c2b7c081cc1b0f5b61ae8ef8224cd9851 # v1
    secrets:
      gh_pat: ${{ secrets.GH_PAT }}
